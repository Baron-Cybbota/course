@model IEnumerable<course.Models.Administrator>

@{
    ViewData["Title"] = "Управление Администраторами";
}

<h1>Управление Администраторами</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Создать нового администратора</a>
</p>

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success mt-3">@TempData["SuccessMessage"]</div>
}
@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger mt-3">@TempData["ErrorMessage"]</div>
}

<table class="table">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(model => model.IdAdministrator)</th>
            <th>@Html.DisplayNameFor(model => model.IdUser)</th>
            <th>@Html.DisplayNameFor(model => model.AssignmentDate)</th>
            <th>Действия</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.IdAdministrator)</td>
                <td>@Html.DisplayFor(modelItem => item.IdUser)</td>
                <td>@Html.DisplayFor(modelItem => item.AssignmentDate)</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.IdAdministrator" class="btn btn-sm btn-warning">Редактировать</a> |
                    <a asp-action="Details" asp-route-id="@item.IdAdministrator" class="btn btn-sm btn-info">Детали</a> |
                    <form asp-action="Delete" asp-route-id="@item.IdAdministrator" method="post" style="display:inline;">
                        <button type="submit" class="btn btn-sm btn-danger ms-2">Удалить</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>